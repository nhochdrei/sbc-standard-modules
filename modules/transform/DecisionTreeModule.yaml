version: "1.0"

name: DecisionTreeModule
inputs:
  - name: values
    size: elemCount
    vars:
      - name: source
        size: featureCount
        operationalType:
          - variant:
              - CATEGORICAL
              - ORDINAL
              - CONTINUOUS
        dataType:
          - variant:
              - INTEGER
              - FLOAT
              - STRING
      - name: target
        size: 1
        operationalType:
          - CATEGORICAL
        dataType:
          - STRING
outputs:
  - name: nodes
    size: nodeCount
    vars:
      - name: node
        size: 1
        operationalType:
          - CATEGORICAL
        dataType:
          - INTEGER
        unique: true
      - name: children
        size: 2
        operationalType:
          - CATEGORICAL
        dataType:
          - INTEGER
        reference: outputs.nodes.node
      - name: feature
        size: 1
        operationalType:
          - CATEGORICAL
        dataType:
          - STRING