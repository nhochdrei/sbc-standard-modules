version: "1.0"

name: MapStringModule
type: TRANSFORM
parameters:
  - key: source
    type: LIST
    dataType: STRING
  - key: target
    type: LIST
    dataType: STRING
inputs:
  - name: original
    size: inputSize
    vars:
      - name: values
        size: inputTimes
        operationalType:
          - CATEGORICAL
        dataType:
          - STRING
outputs:
  - name: mapped
    size: inputSize
    vars:
      - name: values
        size: inputTimes
        operationalType:
          - CATEGORICAL
        dataType:
          - STRING
