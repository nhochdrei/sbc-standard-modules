version: "1.0"

name: MapStringModule
type: TRANSFORM
parameters:
  - key: source
    type: LIST
    operationalType:
      - CATEGORICAL
    dataType:
      - STRING
    required: true
  - key: target
    type: LIST
    operationalType:
      - CATEGORICAL
    dataType:
      - STRING
    required: true
inputs:
  - name: original
    size:
      amount: inputSize
    vars:
      - name: values
        size:
          amount: inputTimes
        operationalType:
          - CATEGORICAL
        dataType:
          - STRING
outputs:
  - name: mapped
    size:
      amount: inputSize
    vars:
      - name: values
        size:
          amount: inputTimes
        operationalType:
          - CATEGORICAL
        dataType:
          - STRING
