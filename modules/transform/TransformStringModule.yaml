version: "1.0"

name: "TransformStringModule"
parameters:
  - key: action
    type: LIST
    dataType: STRING
    pattern: "(trim)|(lower)|(upper)"
    required: true
inputs:
  - name: original
    size: inputSize
    vars:
      - name: values
        size: inputTimes
        operationalType:
          - CATEGORICAL
        dataType:
          - STRING
outputs:
  - name: transformed
    size: inputSize
    vars:
      - name: values
        size: inputTimes
        operationalType:
          - CATEGORICAL
        dataType:
          - STRING
